Микросервис для расписания врачей. Умеет создавать разовые и периодические задачи.

## Что под капотом (Периодичность)
Считаем даты через `rrule`. В настройках можно задать:
* Дату старта
* Тип повторения
* Сколько раз повторять
* До какой даты повторять

**Как решена проблема с выходящими за пределы месяца датами:** если выбрали 29, 30 или 31 число, а в месяце дней меньше (например, в феврале), задача автоматом ставится на последний день месяца.

## Эндпоинты
**Списки и серии:**
* `POST /api/v1/tasks/` — создать разовую задачу или серию (передав `recurrence`).
* `GET /api/v1/tasks/` — посмотреть расписание (с фильтрами по датам, чтобы не класть базу).
* `DELETE /api/v1/tasks/templates/{template_id}` — удалить шаблон и все его невыполненные задачи разом.

**Одиночные задачи:**
* `GET /api/v1/tasks/{task_id}` — получить одну задачу.
* `PUT /api/v1/tasks/{task_id}` — обновить конкретную задачу (изменить текст или статус).
* `DELETE /api/v1/tasks/{task_id}` — удалить только одну задачу (разовую или одну из серии).

## Подключение к БД
Подставьте нужные значения user, pass, ip, port, db_name и добваьте DATABASE_URL в variables
```bash
DATABASE_URL = postgresql+psycopg://user:pass@ip:port/bd_name
```

## Запуск

**Шаг 1.** Базу данных берем из оригинального задания. Сначала поднимите его контейнеры:
```bash
git clone https://github.com/medods/test-task-for-junior-backend-developer
cd test-task-for-junior-backend-developer
docker compose up -d
```

**Шаг 2.** Запускаем наш проект (он сам найдет сеть базы и накатит миграции):
```bash
docker-compose up --build
```

**Swagger (Документация API):**
После запуска всё можно удобно потыкать в браузере: [http://localhost:8000/docs](http://localhost:8000/docs)

## Тесты
```bash
docker exec -it task_tracker_app pytest
```