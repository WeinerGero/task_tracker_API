# Ограничение на количество генерируемых дат, чтобы избежать бесконечных циклов
MAX_COUNT = 365 

# Списки четных и нечетных дней месяца для генерации соответствующих дат
EVEN_DAYS = [day for day in range(1, 32) if day % 2 == 0]
ODD_DAYS = [day for day in range(1, 32) if day % 2 != 0]